// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://lucas-ao6.github.io',
  base: '/CogitoLab',
});

// PRA RODAR NO LOCAL
// import { defineConfig } from 'astro/config';
// export default defineConfig({});
